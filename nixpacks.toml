[phases.setup]
nixPkgs = ['php81', 'php81Extensions.pdo', 'php81Extensions.sqlite3', 'php81Extensions.curl', 'php81Extensions.json', 'php81Extensions.session', 'nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['echo "Build completed - ready for deployment"']

[start]
cmd = 'php -S 0.0.0.0:$PORT index.php'
